function sobrenome(sob) {
    return "Ulysses " + sob
}

const sobrenome = sob => "Ulysses " + sob

const sobrenome = (sob) => "Ulysses " + sob

const sobrenome = (sob) => {
    return "Ulysses " + sob
}

const sobrenome = (sob) => {
    let nomeCompleto = "Ulysses " + sob
    return nomeCompleto
}

console.log(sobrenome("Augusto"))